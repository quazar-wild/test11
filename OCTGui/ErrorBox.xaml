<Window x:Class="OCTGui.ErrorBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OCTGui"
        xmlns:conv ="clr-namespace:OCTGui.Converters"
        mc:Ignorable="d"
        Title="ErrorBox" Height="300" Width="600" WindowStyle="ToolWindow" ResizeMode="NoResize" ShowInTaskbar="False" WindowStartupLocation="CenterOwner"
        >

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisConverter"/>
        <conv:BoolToVisibilityConverter x:Key="InvBoolToVisConverter" />
    </Window.Resources>
    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="50"></RowDefinition>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Grid.Column="0" Grid.RowSpan="2" Background="LightGray">

            <Image Width="48" Height="48" Source="{Binding ErrorIcon}" Margin="10"/>
        </Grid>

        <Grid Grid.Row="0" Grid.Column="1" Visibility="{Binding ContWithoutOCTBool, Converter={StaticResource InvBoolToVisConverter}}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Width="350" Margin="0,3" Text="{Binding ErrorMessage}" FontSize="14" TextWrapping="Wrap" ></TextBlock>
            </StackPanel>
        </Grid>
        
        <Grid Grid.Row="0" Grid.Column="1" Visibility="{Binding ContWithoutOCTBool,Converter={StaticResource BoolToVisConverter}}">
            <TextBlock HorizontalAlignment="Center" FontWeight="Bold" Margin="0,15" Text="Continue without OCT?" FontSize="18"></TextBlock>
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="Enter Password to continue without OCT" FontSize="16" ></TextBlock>
                <PasswordBox Background="#f2f2f2" HorizontalAlignment="Center" Margin="0,5" Width="250" Height="30" PasswordChanged="PasswordBox_PasswordChanged">
                    <PasswordBox.Style>
                        <Style TargetType="PasswordBox">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ContWithoutOCTBool}" Value="True">
                                    <Setter Property="FocusManager.FocusedElement" Value="{Binding RelativeSource={RelativeSource Self}}"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </PasswordBox.Style>
                </PasswordBox>
                <CheckBox IsChecked="{Binding ContWithProcess, Mode=TwoWay}" Margin="10" Content="Continue process"></CheckBox>
            </StackPanel>
        </Grid>
        <Grid Grid.Row="1" Background="LightGray" Grid.ColumnSpan="2">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Width="200" Height="30" Margin="10" Content="Continue without OCTProZ" FontSize="14" 
                        Visibility="{Binding ItIsOct, Converter={StaticResource BoolToVisConverter}}" Command="{Binding ContWithoutOCT}" IsDefault="{Binding ItIsOct}"></Button>
                <Button Margin="5" Content="OK" Width="100" Height="30" FontSize="14"
                        Visibility="{Binding ContWithoutOCTBool,Converter={StaticResource BoolToVisConverter}}" Command="{Binding DeactivateOct}" IsDefault="{Binding ContWithoutOCTBool}"></Button>
                <Button Width="100" Height="30" Margin="10" Content="Cancel" FontSize="14" IsCancel="True" IsDefault="{Binding IsNotOct}"></Button>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
